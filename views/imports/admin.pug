.section
  .section__text
    h1 SMR Academy Admin Dashboard
    p.user-row
      | Admin: 
      span.address-line#admin-address-line address retrieval failed

.section
  .section__text
    .table-header
      h2 Enrollment Information
      label(for="active-only-toggle").btn.btn__action active only
    #admin-table.vr.rows
      h4 loading tenures...
      input(type="checkbox")#active-only-toggle.active-only-toggle
  
.section
  .section__text
    h2 Initialize Tenures
    form(method="post")#form.vr
      input(name="horse_owner", type="text", placeholder="horse owner (address)")
      input(name="academy_mode", type="number", placeholder="academy mode")
      input(name="initial_supply", type="number", placeholder="initial $DERC supply")
      input(name="tenure_length", type="number", placeholder="length (in seconds)")
      input(type="submit" value="initialize tenure" class="btn")